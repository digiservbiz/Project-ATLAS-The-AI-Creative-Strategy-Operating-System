export type HypothesisStatus = "proposed" | "testing" | "supported" | "rejected" | "inconclusive";
export interface CreativeHypothesis { id:string; businessId:string; statement:string; reasoning:string[]; evidenceIds:string[]; variable:string; control?:string; variants:string[]; successMetrics:string[]; expectedOutcome:string; status:HypothesisStatus; confidence:number; createdAt:string; }
export function createCreativeHypothesis(input:CreativeHypothesis):CreativeHypothesis { if(!input.statement.trim()) throw new Error("Hypothesis statement is required"); if(!input.variable.trim()) throw new Error("A test variable is required"); if(input.variants.length<2) throw new Error("A hypothesis needs at least two variants"); if(input.confidence<0||input.confidence>1) throw new Error("Hypothesis confidence must be between 0 and 1"); return structuredClone(input); }
