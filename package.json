{
  "name": "atlas-ai",
  "version": "0.3.0",
  "private": true,
  "description": "ATLAS — AI Creative Strategy Operating System",
  "packageManager": "pnpm@10.14.0",
  "scripts": {
    "dev": "pnpm --filter @atlas/api dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "typescript": "^5.9.2"
  }
}
